// import { ComponentFixture, TestBed } from '@angular/core/testing';

import { StickerMessageComponent } from './sticker-message.component';

// const clickCloseIcon: boolean = true;

describe('StickerMessageComponent', () => {
  // let component: StickerMessageComponent;
  // let fixture: ComponentFixture<StickerMessageComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ StickerMessageComponent ]
  //   })
  //   .compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(StickerMessageComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });


  it('should create', () => {
    const form = new StickerMessageComponent();
    expect(form).toBeTruthy();
  });

  // // it('Sticker component is close', () => {
  // //   const action = 'buttonClose'


  // // });

  // // close() {
  // //   this.closeAlert.emit(true)
  // // }
});
